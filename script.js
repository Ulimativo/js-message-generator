// Topic?
// 3 sets of data = parts of the sentence.

const messageComponents = {
  helpverb: ["should", "need to", "must"],
  verb: ["work", "learn", "relax"],
  end: ["more often", "always", "never"],
};

console.log(messageComponents.helpverb);
